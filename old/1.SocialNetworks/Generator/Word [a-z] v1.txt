#Вариант 1
# Выводит все комбинации из 3х букв
letter=["ago"]


from string import ascii_lowercase

for i in ascii_lowercase:
    print(i+letter[0][1:])
    print(letter[0][:1]+i+letter[0][2:])
    print(letter[0][:2]+i)
    
# Выводит: ago, aao, aga, bgo.....agz